apiVersion: v1
kind: Service
metadata:
  name: {{ include "haztrak.fullname" . }}
  labels:
    {{- include "haztrak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "haztrak.selectorLabels" . | nindent 4 }}
