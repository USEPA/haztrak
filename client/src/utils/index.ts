// Todo: eliminate this directory, i.e., find a better place for test-wrapper and
//  remove the history object (POC source)
import history from './history';

export * from './test-wrapper';

export default history;
